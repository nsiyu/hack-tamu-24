import React from "react";
import { Checkbox, Layout, Row, Col, Button } from "antd";

const style = {
  background: "#0092ff",
  padding: "8px 0",
};

const SelectInterest = () => {
  return (
    <Row gutter={[16, 16]}>
      <Col span={8} >
        hi
        </Col>

      <Col span={8} />
      <Col span={8} />

      <Col span={8} />
      <Col span={8} />
      <Col span={8} />
    </Row>
  );
};

export default SelectInterest;
